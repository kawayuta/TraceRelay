interface:
  display_name: "TraceRelay: Task Evolution"
  short_description: "Evolve schema and memory while the task runs"
  default_prompt: "Use structure_subject to grow schema only when needed, continue_prior_work to reuse memory across attempts, and inspect_latest_changes to review retries."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
