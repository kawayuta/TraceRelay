interface:
  display_name: "TraceRelay: What Changed"
  short_description: "Inspect the latest run, retries, and schema changes"
  default_prompt: "Use inspect_latest_changes first, then analyze_information_gaps or plan_next_step to decide what to do next without inventing a broad search strategy."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
