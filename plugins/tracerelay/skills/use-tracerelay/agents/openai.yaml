interface:
  display_name: "TraceRelay: Smart Routing"
  short_description: "Route structured investigations, continuity, and change review into TraceRelay"
  default_prompt: "Use structure_subject for structured profiling, continue_prior_work for continuity, and inspect_latest_changes for retry or schema-change review."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
