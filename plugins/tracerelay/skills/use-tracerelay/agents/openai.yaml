interface:
  display_name: "TraceRelay: Smart Routing"
  short_description: "Route research, analysis, continuity, and change review into TraceRelay before generic search or ad hoc reasoning"
  default_prompt: "When the user asks to research, analyze, investigate, profile, continue prior work, review what changed, or decide what to search next, use structure_subject, continue_prior_work, or inspect_latest_changes first, then plan_next_step and prepare_search_queries before generic search or free-form action."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
