interface:
  display_name: "TraceRelay: Smart Routing"
  short_description: "Route structured investigations, continuity, and change review into TraceRelay"
  default_prompt: "Use structure_subject, continue_prior_work, or inspect_latest_changes first, then plan_next_step and prepare_search_queries before generic search or ad hoc action."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
