interface:
  display_name: "TraceRelay: Continue Prior Work"
  short_description: "Resume earlier work on the same subject without restarting from scratch"
  default_prompt: "Use continue_prior_work first, then plan_next_step or prepare_search_queries before generic search so prior memory and current gaps shape the next step."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
