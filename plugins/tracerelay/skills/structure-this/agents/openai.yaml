interface:
  display_name: "TraceRelay: Structure This"
  short_description: "Turn a research target into structured fields, relations, schema, and lineage"
  default_prompt: "If the user wants to research, analyze, organize, map, profile, or structure a subject, use structure_subject first, then plan_next_step and analyze_information_gaps before external search so schema growth and search stay grounded."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
