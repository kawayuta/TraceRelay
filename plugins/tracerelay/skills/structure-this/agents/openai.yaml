interface:
  display_name: "TraceRelay: Structure This"
  short_description: "Turn a subject into structured fields, relations, and lineage"
  default_prompt: "Use structure_subject first, then plan_next_step and analyze_information_gaps before external search so schema growth and search stay grounded."

dependencies:
  tools:
    - type: "mcp"
      value: "TraceRelay"
      description: "TraceRelay MCP server"
